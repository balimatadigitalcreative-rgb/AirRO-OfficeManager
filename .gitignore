# Dependencies
node_modules/

# Databases & secrets (never commit these)
*.db
*.db-journal
.env
.env.local
.env.*.local

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Local rollback backups (durable backup is git history)
*.html.bak
_built-test.html

# NOTE: dist/ IS committed on purpose — it is the built frontend bundle the VPS serves.

# Deploy pipeline log (generated on the server, never committed)
deploy/deploy.log
